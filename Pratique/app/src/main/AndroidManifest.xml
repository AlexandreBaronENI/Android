<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="alexandre.pratique">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Pratique">
        <activity android:theme="@style/Theme.AppCompat.DayNight.NoActionBar" android:name=".CategoryAddActivity"></activity>
        <activity android:theme="@style/Theme.AppCompat.DayNight.NoActionBar" android:name=".ArticleAddActivity"></activity>
        <activity android:theme="@style/Theme.AppCompat.DayNight.NoActionBar" android:name=".CategoryDetailActivity" />
        <activity android:theme="@style/Theme.AppCompat.DayNight.NoActionBar" android:name=".CategoriesListActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:theme="@style/Theme.AppCompat.DayNight.NoActionBar" android:name=".ArticlesListAcivity" />
        <activity android:theme="@style/Theme.AppCompat.DayNight.NoActionBar" android:name=".ArticleDetailActivity" />
    </application>

</manifest>